{
  "name": "jacs-pos",
  "version": "2.1.0",
  "description": "JACS POS - AI-Powered Point of Sale with QuickBooks Online",
  "type": "module",
  "scripts": {
    "dev": "node server/server.js",
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "openai": "^4.28.0",
    "cookie-parser": "^1.4.6"
  }
}
